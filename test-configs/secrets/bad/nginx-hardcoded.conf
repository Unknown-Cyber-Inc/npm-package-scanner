# nginx config with hardcoded secrets - SHOULD TRIGGER ALERTS

server {
    listen 80;
    server_name example.com;

    # Database connection with hardcoded password
    set $db_host "mysql.internal";
    set $db_user "admin";
    set $db_password "SuperSecret123!";

    # API key in header
    proxy_set_header X-API-Key "sk_live_abcdef123456789";

    location /api {
        proxy_pass http://backend:8080;
        proxy_set_header Authorization "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.secret";
    }
}

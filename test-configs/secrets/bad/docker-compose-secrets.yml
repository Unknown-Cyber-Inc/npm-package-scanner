# docker-compose with hardcoded secrets - SHOULD TRIGGER ALERTS
# NOTE: These are FAKE example values for testing, not real credentials

version: '3.8'

services:
  database:
    image: postgres:14
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: "MyDatabasePassword123"
      
  redis:
    image: redis:7
    command: redis-server --requirepass "RedisSecret456"
    
  app:
    image: myapp:latest
    environment:
      DATABASE_URL: "postgresql://admin:MyDatabasePassword123@database:5432/app"
      # AWS example keys (documented fake values from AWS docs)
      AWS_ACCESS_KEY_ID: "AKIAIOSFODNN7EXAMPLE"
      AWS_SECRET_ACCESS_KEY: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
      # Fake API keys for testing pattern detection
      API_SECRET_KEY: "test_secret_key_12345_fake"
      PRIVATE_TOKEN: "fake_token_for_testing_only"
